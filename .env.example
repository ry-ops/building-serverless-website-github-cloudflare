# Public Environment Variables (available in browser)
PUBLIC_SITE_URL=https://your-site.pages.dev

# Private Environment Variables (server-only)
# Add your private variables here (never commit the actual .env file)
